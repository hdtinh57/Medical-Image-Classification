# Promtail — đọc log mọi container Docker qua daemon API rồi đẩy vào Loki.
# Gắn nhãn theo tên container để lọc trong Grafana: {container="triton"}.
server:
  http_listen_port: 9080
  grpc_listen_port: 0

positions:
  filename: /tmp/positions.yaml

clients:
  - url: http://loki:3100/loki/api/v1/push

scrape_configs:
  - job_name: docker
    docker_sd_configs:
      - host: unix:///var/run/docker.sock
        refresh_interval: 5s
    relabel_configs:
      # /triton -> container="triton"
      - source_labels: ["__meta_docker_container_name"]
        regex: "/(.*)"
        target_label: container
      # phân biệt stdout/stderr
      - source_labels: ["__meta_docker_container_log_stream"]
        target_label: stream
      # nhãn compose project để lọc theo cụm
      - source_labels: ["__meta_docker_container_label_com_docker_compose_project"]
        target_label: compose_project
